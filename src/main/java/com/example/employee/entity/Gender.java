package com.example.employee.entity;

/**
 * Enum đại diện cho giới tính của nhân viên.
 * Được ánh xạ vào CSDL dưới dạng chuỗi (EnumType.STRING).
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHER
}
